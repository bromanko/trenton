Starts the web server for development

Options:
    -l                Runs commands locally rather than in Docker containers
    -d                When running in docker will start the service in the background
